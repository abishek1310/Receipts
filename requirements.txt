# Receipts — runtime dependencies (CLAUDE.md §3). Python 3.11+.
# This is what Streamlit Community Cloud installs. Keep it lean; ingest and test
# tooling lives in requirements-dev.txt.

# Orchestration
langgraph==1.2.11
langchain-core==1.6.3

# LLM provider. The adapter in src/llm.py is swappable; uncomment openai to use it.
anthropic==1.7.0
# openai==2.9.0

# Data + config
pydantic==2.13.5
pydantic-settings==2.15.0

# UI
streamlit==1.64.0
